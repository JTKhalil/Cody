---
description: 修改 sketch_apr15a 固件后自动编译并烧录（arduino-cli）
globs: sketch_apr15a/**/*
alwaysApply: false
---

# ESP32 固件：改完即编译 + 烧录

在编辑 `sketch_apr15a/` 下任意固件相关文件（`.ino`、`.cpp`、`.h`、`.csv` 等）后，**除非用户明确说不要烧录**，须在本轮任务内亲自执行：

1. **compile** — 必须通过  
2. **upload** — 烧录成功为止；失败则换思路重试并说明原因  

不能只提交代码差分而不跑编译/烧录。

## 命令（与仓库根 `README.md` 一致）

- **arduino-cli**：`"C:\Program Files\Arduino CLI\arduino-cli.exe"`  
- **草图目录**：与本仓库一致时为 `d:\mochi\Cody\sketch_apr15a`（按本机实际路径替换）  
- **FQBN**：

`esp32:esp32:esp32c3:CDCOnBoot=cdc,CPUFreq=160,FlashFreq=80,FlashMode=dio,FlashSize=4M,JTAGAdapter=default,PartitionScheme=min_spiffs,UploadSpeed=921600,EraseFlash=none,ZigbeeMode=default,DebugLevel=none`

**编译：**

```powershell
& "C:\Program Files\Arduino CLI\arduino-cli.exe" compile --fqbn "esp32:esp32:esp32c3:CDCOnBoot=cdc,CPUFreq=160,FlashFreq=80,FlashMode=dio,FlashSize=4M,JTAGAdapter=default,PartitionScheme=min_spiffs,UploadSpeed=921600,EraseFlash=none,ZigbeeMode=default,DebugLevel=none" "d:\mochi\Cody\sketch_apr15a"
```

**烧录**（端口以 `arduino-cli board list` 为准，常见 `COM5`）：

```powershell
& "C:\Program Files\Arduino CLI\arduino-cli.exe" upload -p COM5 --fqbn "esp32:esp32:esp32c3:CDCOnBoot=cdc,CPUFreq=160,FlashFreq=80,FlashMode=dio,FlashSize=4M,JTAGAdapter=default,PartitionScheme=min_spiffs,UploadSpeed=921600,EraseFlash=none,ZigbeeMode=default,DebugLevel=none" "d:\mochi\Cody\sketch_apr15a"
```

若 **port is busy**：提示关闭 WebSerial/串口监视器等占用 COM 的软件后重试。

## 例外

仅改 `index.html` / 文档且未改固件；或用户写明不要编译/烧录。
