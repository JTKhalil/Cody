---
description: 修改 ESP32 固件后必须重新编译并烧录
alwaysApply: true
---

# ESP32 固件修改后的强制流程

当本次会话**对 ESP32 固件代码有任何修改**（尤其是 `sketch_apr15a/**` 下的 `.ino/.h/.cpp/.csv`）时，在交付/结束前必须执行：

- 先 **compile**（确认能编译通过）
- 再 **upload**（烧录到板子）

## 约束

- 不能只改代码不验证；必须以 `arduino-cli` 编译与烧录结果为准。
- 如果烧录失败且提示端口占用（如 COM5 busy），先提示用户释放占用（浏览器 WebSerial/串口监视器等），然后继续重试烧录。

