---
description: 修改 sketch_apr15a 固件后自动编译并烧录（arduino-cli）
globs: sketch_apr15a/**/*
alwaysApply: false
---

# ESP32 固件：改完即编译 + 烧录

在编辑 `sketch_apr15a/` 下任意固件相关文件（`.ino`、`.cpp`、`.h`、`.csv` 等）后，**除非用户明确说不要烧录**，须在本轮任务内亲自执行：

1. **compile** — 必须通过  
2. **upload** — 烧录成功为止；失败则换思路重试并说明原因  

不能只提交代码差分而不跑编译/烧录。

## 命令（与 `sketch_apr15a/flash.ps1` 中 `$Fqbn` 完全一致）

**优先**：在 `sketch_apr15a/` 下执行 `.\flash.ps1`（或 `.\flash.ps1 -Port COMx`），与人工烧录流程一致。

**FQBN**（整段一行，与 `flash.ps1` 同步维护）：

`esp32:esp32:esp32c3:CDCOnBoot=cdc,CPUFreq=160,FlashFreq=80,FlashMode=dio,FlashSize=4M,JTAGAdapter=default,PartitionScheme=huge_app,UploadSpeed=256000,EraseFlash=none,ZigbeeMode=default,DebugLevel=none`

- **arduino-cli**（可选完整路径）：`"C:\Program Files\Arduino CLI\arduino-cli.exe"`  
- **草图目录**：按本机实际路径，例如 `d:\mochi\CodyProject\Cody\sketch_apr15a`

**编译：**

```powershell
$Fqbn = 'esp32:esp32:esp32c3:CDCOnBoot=cdc,CPUFreq=160,FlashFreq=80,FlashMode=dio,FlashSize=4M,JTAGAdapter=default,PartitionScheme=huge_app,UploadSpeed=256000,EraseFlash=none,ZigbeeMode=default,DebugLevel=none'
arduino-cli compile "--fqbn=$Fqbn" "d:\mochi\CodyProject\Cody\sketch_apr15a"
```

**烧录**（端口以 `arduino-cli board list` 为准）：

```powershell
arduino-cli upload "d:\mochi\CodyProject\Cody\sketch_apr15a" -p COM5 "--fqbn=$Fqbn"
```

若 **port is busy**：提示关闭 WebSerial/串口监视器等占用 COM 的软件后重试。

## 例外

仅改 `index.html` / 文档且未改固件；或用户写明不要编译/烧录。
